import Typography from "@mui/material/Typography";

/* Returns 404 page for routes that do not exist on the Hub */
const NotFound = () => {
  return (
    <Typography>404 page</Typography>
  ); 
};

export default NotFound;
